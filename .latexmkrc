# Use xelatex for the compilation process
$pdf_mode = 1;
$pdflatex = 'xelatex --shell-escape %O %S';

# Custom dependencies or additional settings
$clean_ext = 'synctex.gz nav snm thm soc loc glg acn vrb';

# End of .latexmkrc

